from enum import Enum
class Unit(Enum):
    #this enum is used to store units. Each unit will also have diferent values that indicates from what they can be converted to.
    farenheightToCelsius=1
    celsiusToFarenheight=2
